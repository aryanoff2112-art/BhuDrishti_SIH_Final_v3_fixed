# Copy this file to .env and set strong, environment-specific secrets.
# Never commit or submit the real .env file.
GEMINI_API_KEY=replace_with_your_provider_key
GEMINI_MODEL=gemini-2.5-flash
JWT_SECRET=replace_with_a_random_64_character_secret
APP_URL=http://localhost:3000
